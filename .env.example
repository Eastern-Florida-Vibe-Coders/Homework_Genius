# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: demo login ("Try demo account" on login/signup).
# In development, defaults are used if unset: demo@homeworkgenius.local / demo-homework-genius
# Service role is used once to create that user with email already confirmed (no inbox).
DEMO_USER_EMAIL=
DEMO_USER_PASSWORD=
